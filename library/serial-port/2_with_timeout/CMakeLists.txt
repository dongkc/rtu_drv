#AUX_SOURCE_DIRECTORY(. SERIAL_SRC)

#ADD_LIBRARY(serial STATIC ${SERIAL_SRC})
ADD_LIBRARY(serial STATIC TimeoutSerial.cpp)

LINK_DIRECTORIES(${PROJECT_BINARY_DIR}/lib)

#TARGET_LINK_LIBRARIES(utils pthread)
