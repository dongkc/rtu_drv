INCLUDE_DIRECTORIES(.)
INCLUDE_DIRECTORIES(../../../include)
INCLUDE_DIRECTORIES(../../../qf/source)

AUX_SOURCE_DIRECTORY(. QPPORT_SRC)
ADD_LIBRARY(qpport STATIC ${QPPORT_SRC})

LINK_DIRECTORIES(${PROJECT_BINARY_DIR}/lib)
#TARGET_LINK_LIBRARIES(qpport libqf.a)
